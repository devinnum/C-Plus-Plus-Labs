#include <iostream>

using namespace std;

int binaryBitCounter(int n) {
  int count = 0;
  if (n % 2 == 0) {
    bitBinaryCounter(n / 2)
  }
  else {
    count++;
    bitBinaryCounter(n / 2)
  }
  return count;
}
