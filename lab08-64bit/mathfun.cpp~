#include <iostream>
#include <cstdlib>

using namespace std;

extern "C" long product (long, long);

int main() {
  long x, y;
  cout << "Enter integer 1: ";
  cin >> x;
  cout << "Enter integer 2: ";
  cin >> y;
  cout << product(x, y);
  return 0;
}
